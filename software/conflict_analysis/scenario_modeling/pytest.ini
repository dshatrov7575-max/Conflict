[pytest]
DJANGO_SETTINGS_MODULE = player_integration.settings
pythonpath = ..
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    postgres: requires a PostgreSQL database
