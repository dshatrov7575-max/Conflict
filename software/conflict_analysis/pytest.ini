[pytest]
DJANGO_SETTINGS_MODULE = conflict_analysis.settings
python_files = test_*.py tests.py
testpaths = domain/tests
addopts = -ra --strict-markers
markers =
    postgres: requires a PostgreSQL database
